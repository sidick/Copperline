name: Libretro

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

concurrency:
  group: libretro-${{ github.ref }}
  cancel-in-progress: true

jobs:
  core:
    name: Libretro (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            library: libcopperline_libretro.so
            installed: copperline_libretro.so
          - os: macos-latest
            library: libcopperline_libretro.dylib
            installed: copperline_libretro.dylib
          - os: windows-latest
            library: copperline_libretro.dll
            installed: copperline_libretro.dll
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: actions/setup-python@v7
        with:
          python-version: "3.x"
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.ref == 'refs/heads/main' }}
          workspaces: crates/copperline-libretro -> target
      - name: Formatting and lint
        run: |
          cargo fmt --manifest-path crates/copperline-libretro/Cargo.toml --check
          cargo clippy --manifest-path crates/copperline-libretro/Cargo.toml --all-targets --locked -- -D warnings
      - name: Fetch WHDLoad support archives
        run: tools/fetch-whdload.sh
      - name: Deterministic frontend tests
        run: |
          cargo test --manifest-path crates/copperline-libretro/Cargo.toml --profile ci --locked
          cargo test --manifest-path crates/copperline-libretro/Cargo.toml --profile ci --locked whdload_stages -- --ignored
      - name: Build release core
        run: cargo build --manifest-path crates/copperline-libretro/Cargo.toml --release --locked
      - name: Shared-library ABI and replay
        run: |
          python tools/check-libretro.py "crates/copperline-libretro/target/release/${{ matrix.library }}" --screenshot libretro.png
          python tools/check-libretro.py "crates/copperline-libretro/target/release/${{ matrix.library }}" --model A1200 --video NTSC
      - name: RetroArch rollback netplay
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y retroarch xvfb libxtst6
          python tools/check-libretro-netplay.py "crates/copperline-libretro/target/release/${{ matrix.library }}" --output netplay-test
      - name: Netplay logs
        if: always() && runner.os == 'Linux'
        uses: actions/upload-artifact@v7
        with:
          name: libretro-netplay-logs
          path: netplay-test/*/*.log
      - name: Package core
        run: |
          mkdir -p libretro-package/aros libretro-package/system/whdboot
          cp assets/whdboot/*.lha libretro-package/system/whdboot/
          cp "crates/copperline-libretro/target/release/${{ matrix.library }}" "libretro-package/${{ matrix.installed }}"
          cp crates/copperline-libretro/copperline_libretro.info libretro-package/
          cp LICENSE libretro-package/
          cp assets/aros/LICENSE assets/aros/ACKNOWLEDGEMENTS assets/aros/README.md libretro-package/aros/
          cp docs/guide/libretro.md libretro-package/README.md
      - uses: actions/upload-artifact@v7
        with:
          name: copperline-libretro-${{ runner.os }}-${{ runner.arch }}
          path: libretro-package/
      - uses: actions/upload-artifact@v7
        with:
          name: libretro-capture-${{ runner.os }}
          path: libretro.png

  release:
    name: Attach cores to release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: core
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: copperline-libretro-*
          path: cores
      - name: Archive each platform
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          python3 - <<'PY'
          import os
          from pathlib import Path
          import shutil

          version = os.environ["RELEASE_TAG"].removeprefix("v")
          packages = sorted(Path("cores").glob("copperline-libretro-*"))
          if len(packages) != 3:
              raise SystemExit(f"Expected three core packages, found {len(packages)}")
          Path("release-assets").mkdir()
          for package in packages:
              platform = package.name.removeprefix("copperline-libretro-")
              shutil.make_archive(
                  f"release-assets/Copperline-{version}-libretro-{platform}",
                  "zip", package,
              )
          PY
      - uses: softprops/action-gh-release@v3
        with:
          files: release-assets/*.zip
          fail_on_unmatched_files: true
